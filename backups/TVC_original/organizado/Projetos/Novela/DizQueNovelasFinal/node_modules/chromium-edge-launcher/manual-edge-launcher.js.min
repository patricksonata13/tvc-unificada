#!/usr/bin/env node
"use strict";require("./compiled-check.js")("./dist/chromium-edge-launcher.js");const{Launcher:Launcher,launch:launch}=require("./dist/chromium-edge-launcher"),args=process.argv.slice(2),edgeFlags=[];let startingUrl,port,ignoreDefaultFlags;if(args.length){const e=args.filter(e=>e.startsWith("--")),r=e.find(e=>e.startsWith("--port="));r&&(port=parseInt(r.replace("--port=",""),10));!!e.find(e=>"--enable-extensions"===e)&&(ignoreDefaultFlags=!0,edgeFlags.push(...Launcher.defaultFlags().filter(e=>"--disable-extensions"!==e))),edgeFlags.push(...e),startingUrl=args.find(e=>!e.startsWith("--"))}launch({startingUrl:startingUrl,port:port,ignoreDefaultFlags:ignoreDefaultFlags,edgeFlags:edgeFlags}).then(e=>console.log(`✨  Edge debugging port: ${e.port}`));