import{now,round}from"../utils/utils-consts";import getCenter from"./get-center";export default function simpleCloneInputData(t){let e=[],n=0;for(;n<t.pointers.length;)e[n]={clientX:round(t.pointers[n].clientX),clientY:round(t.pointers[n].clientY)},n++;return{timeStamp:now(),pointers:e,center:getCenter(e),deltaX:t.deltaX,deltaY:t.deltaY}}