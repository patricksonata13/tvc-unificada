#!/usr/bin/env node
"use strict";require("./compiled-check.js")("./dist/chrome-launcher.js");const{Launcher:Launcher,launch:launch}=require("./dist/chrome-launcher"),args=process.argv.slice(2),chromeFlags=[];let startingUrl,port,ignoreDefaultFlags;if(args.length){const r=args.filter(r=>r.startsWith("--")),e=r.find(r=>r.startsWith("--port="));e&&(port=parseInt(e.replace("--port=",""),10));!!r.find(r=>"--enable-extensions"===r)&&(ignoreDefaultFlags=!0,chromeFlags.push(...Launcher.defaultFlags().filter(r=>"--disable-extensions"!==r))),chromeFlags.push(...r),startingUrl=args.find(r=>!r.startsWith("--"))}launch({startingUrl:startingUrl,port:port,ignoreDefaultFlags:ignoreDefaultFlags,chromeFlags:chromeFlags}).then(r=>console.log(`✨  Chrome debugging port: ${r.port}`));