"use strict";import{logger}from"../common";export function scrollTo(o,e,l,r){const t=o();if(!t)return void logger.warn("Called scrollTo() with an uninitialized ref. Make sure to pass the animated ref to the scrollable component before calling scrollTo().");const n=t;n?.scrollTo({x:e,y:l,animated:r})}