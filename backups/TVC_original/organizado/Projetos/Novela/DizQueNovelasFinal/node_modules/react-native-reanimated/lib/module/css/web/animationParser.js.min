"use strict";import{hasSuffix}from"../../common";import{PROPERTIES_CONFIG}from"./config";import{createStyleBuilder}from"./style";import{parseTimingFunction}from"./utils";const styleBuilder=createStyleBuilder(PROPERTIES_CONFIG);export function processKeyframeDefinitions(i){return Object.entries(i).reduce((i,[e,r])=>{const t=hasSuffix(e)?e:100*parseFloat(e)+"%",n=processKeyframeBlock(r);return n?(i.push(`${t} { ${n} }`),i):i},[]).join(" ")}function processKeyframeBlock({animationTimingFunction:i,...e}){const r=styleBuilder.buildFrom(e);return r?i?`animation-timing-function: ${parseTimingFunction(i)}; ${r}`:r:null}