"use strict";const semverSatisfies=require("semver/functions/satisfies"),semverPrerelease=require("semver/functions/prerelease"),expectedVersion=require("./worklets-version.json"),compatibilityFile=require("../compatibility.json");function validateVersion(e){let i;try{const{version:e}=require("react-native-worklets/package.json");i=e}catch(i){return{ok:!1,message:"react-native-worklets package isn't installed. Please install a version between "+expectedVersion.min+" and "+expectedVersion.max+" to use Reanimated "+e+"."}}if(semverPrerelease(i))return{ok:!0};const t=[];for(const i in compatibilityFile)semverSatisfies(e,i)&&t.push(...compatibilityFile[i]["react-native-worklets"]);if(0===t.length)return{ok:!0};for(const e of t)if(semverSatisfies(i,e))return{ok:!0};return{ok:!1,message:`Invalid version of \`react-native-worklets\`: "${i}". Expected the version to be in inclusive range "${t.join(", ")}". Please install a compatible version of \`react-native-worklets\`.`}}module.exports=validateVersion;