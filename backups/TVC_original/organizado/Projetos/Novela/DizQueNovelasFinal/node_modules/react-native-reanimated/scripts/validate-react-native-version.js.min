"use strict";const semverSatisfies=require("semver/functions/satisfies"),semverPrerelease=require("semver/functions/prerelease"),path=require("path"),packageJsonPath=path.resolve(__dirname,"../package.json"),packageJson=require(packageJsonPath),reanimatedVersion=packageJson.version,compatibilityFile=require("../compatibility.json"),reactNativeVersion=process.argv[2],supportedRNVersions=[];semverPrerelease(reactNativeVersion)&&process.exit(0);for(const e in compatibilityFile)semverSatisfies(reanimatedVersion,e)&&supportedRNVersions.push(...compatibilityFile[e]["react-native"]);0===supportedRNVersions.length&&process.exit(0);for(const e of supportedRNVersions)semverSatisfies(reactNativeVersion,`${e}.x`)&&process.exit(0);console.error(`[Reanimated] React Native ${reactNativeVersion} version is not compatible with Reanimated ${reanimatedVersion}`),process.exit(1);