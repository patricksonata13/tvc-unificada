"use strict";function compareSnaps(e,t,o,r,n,a,s){const i=Object.keys(e).sort(),p=Object.keys(r).sort(),c=i.filter(e=>-1!==p.indexOf(e));describe("RN Codegen Parsers",()=>{it("should not unintentionally contains test case for Flow but not for TypeScript",()=>{expect(i.filter(e=>-1===c.indexOf(e))).toEqual(o)}),it("should not unintentionally contains test case for TypeScript but not for Flow",()=>{expect(p.filter(e=>-1===c.indexOf(e))).toEqual(a)});for(const e of c){const o=t[`RN Codegen Flow Parser can generate fixture ${e} 1`],r=n[`RN Codegen TypeScript Parser can generate fixture ${e} 1`];it(`should be able to find the snapshot for Flow for case ${e}`,()=>{expect(typeof o).toEqual("string")}),it(`should be able to find the snapshot for TypeScript for case ${e}`,()=>{expect(typeof r).toEqual("string")}),-1===s.indexOf(e)?it(`should generate the same snapshot from Flow and TypeScript for fixture ${e}`,()=>{expect(o).toEqual(r)}):it(`should generate the different snapshot from Flow and TypeScript for fixture ${e}`,()=>{expect(o).not.toEqual(r)})}})}function compareTsArraySnaps(e,t){for(const o of t.filter(e=>-1!==e.indexOf("ARRAY2"))){const t=o.replace("ARRAY2","ARRAY");it(`should generate the same snap from fixture ${t} and ${o}`,()=>{expect(e[`RN Codegen TypeScript Parser can generate fixture ${t}`]).toEqual(e[`RN Codegen TypeScript Parser can generate fixture ${o}`])})}}module.exports={compareSnaps:compareSnaps,compareTsArraySnaps:compareTsArraySnaps};