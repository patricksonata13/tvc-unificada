"use strict";let reporter;const logPath=process.env.RCT_PACKAGER_LOG_PATH;if(null!=logPath&&""!==logPath){const{JsonReporter:e}=require("metro"),r=require("fs"),t=require("path");reporter=class extends e{constructor(){r.mkdirSync(t.dirname(logPath),{recursive:!0}),super(r.createWriteStream(logPath))}}}else reporter=require("metro").TerminalReporter;module.exports=reporter;