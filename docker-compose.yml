version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./tvc.db:/app/tvc.db
      - ./backups:/app/backups
    environment:
      - SECRET_KEY=chave-super-secreta-tvc
      - DEBUG=False
